const express = require('express');
const router = express.Router();

router.get(`candidate/:candidateId/basic-info`, () => {});
router.post(`candidate/:candidateId/basic-info`, () => {});

module.exports = router;
